extends layout

//- include mixins/_courseForm

block content
	#stream
		h2 #{title}
		//- +courseForm(course)
		form(action="" method="POST" class="card")
			label(for="course") Course Name
			input(type="text" name="courseName" id="course" required)
			#dynamicForm
				label(for="time") Enter Time
				input(type="number" name="daytime[time]" min="8" max="16" placeholder="must be between 8 and 16" required) 
				label(for="day") Enter Day
				input(type="text" name="daytime[day]" placeholder="Between Monday and Saturday" required)
			label()
			input(type="button" value="Add another time and day" onclick="addInput('dynamicForm')")
			input(type="submit" value="Save")